{% extends 'sortie/create_sortie.html.twig' %}

{%block nomPage%}<h2>Modifier une sortie</h2>{%endblock%}

    {% block buttons %}
        <div class="buttons">
            {{ form_widget(form.checkpoint) }}
            {{ form_widget(form.publish) }}
            <a href="{{ path("cancel_sortie", {'id':sortie.id}) }}">
                <input class="btn btn-success"
                       type="button"
                       value="Supprimer la sortie">
            </a>
            <a href="{{ path("home") }}">
                <input class="btn btn-success"
                       type="button"
                       value="Annuler">
            </a>
        </div>
    {% endblock %}